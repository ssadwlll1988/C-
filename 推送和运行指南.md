# Flutter C盘卫士 - 推送和运行指南

## 📤 推送到 GitHub

由于当前网络无法直接访问 GitHub，请按以下步骤手动推送：

### 方法一：使用 Git 命令行（推荐）

1. **打开终端/命令提示符**
   ```bash
   cd d:\phpstudy_pro\WWW\disk\Flutter_Project
   ```

2. **确认远程仓库已添加**
   ```bash
   git remote -v
   ```
   应该看到：
   ```
   github  https://github.com/ssadwlll1988/C-.git (fetch)
   github  https://github.com/ssadwlll1988/C-.git (push)
   ```

3. **推送到 GitHub**
   ```bash
   git push github master
   ```

4. **如果提示需要认证**
   - 使用 GitHub Personal Access Token
   - 或者配置 SSH key

### 方法二：使用 GitHub Desktop

1. 下载并安装 [GitHub Desktop](https://desktop.github.com/)
2. 点击 "Add an Existing Repository"
3. 选择 `d:\phpstudy_pro\WWW\disk\Flutter_Project` 文件夹
4. 点击 "Publish repository"
5. 设置仓库名称为 "C-"，描述可选
6. 点击 "Publish"

### 方法三：通过 Gitee 中转

如果已经推送到 Gitee，可以：
1. 在 GitHub 创建新仓库 "C-"
2. 使用 GitHub 的 "Import repository" 功能
3. 输入 Gitee 仓库地址：`https://gitee.com/ssadwlll1988/Flutter.git`
4. 等待导入完成

---

## 💻 在另一台电脑运行

### 前置条件检查清单

在另一台电脑上运行前，请确保已安装：

#### ✅ 必需软件

1. **Git** (用于克隆代码)
   - 下载地址：https://git-scm.com/download/win
   - 验证：`git --version`

2. **Flutter SDK** (>=2.19.0)
   - 下载地址：https://docs.flutter.dev/get-started/install/windows
   - 验证：`flutter --version`
   - **重要**：需要将 Flutter 添加到系统 PATH

3. **Visual Studio 2022** (不是 VS Code)
   - 下载地址：https://visualstudio.microsoft.com/zh-hans/downloads/
   - **必须安装的组件**：
     - ✅ 使用C++的桌面开发 (Desktop development with C++)
     - ✅ Windows 10 SDK (或更高版本)
     - ✅ CMake tools for Windows
   - 验证：`flutter doctor` 应该显示 Visual Studio 相关项为绿色 ✓

4. **Windows 10/11 操作系统**

#### ⚠️ 常见错误及解决方案

**错误 1**: `Visual Studio not found` 或 `incomplete`
```
解决方法：
1. 打开 Visual Studio Installer
2. 修改已安装的 VS 2022
3. 勾选"使用C++的桌面开发"
4. 确保选中了右侧的：
   - MSVC v143 构建工具
   - Windows 10 SDK
   - CMake 工具
5. 点击"修改"安装
```

**错误 2**: `CMake not found`
```
解决方法：
1. 确认已通过 VS Installer 安装 CMake
2. 重启电脑
3. 运行 flutter doctor 检查
```

**错误 3**: `Nuget.exe not found`
```
这是警告，不影响运行，可以忽略
```

**错误 4**: `win32 package errors`
```
解决方法：
在项目目录运行：
flutter clean
flutter pub get
```

### 📋 完整运行步骤

#### 步骤 1: 克隆仓库

```bash
# 从 GitHub 克隆
git clone https://github.com/ssadwlll1988/C-.git
cd C-

# 或者从 Gitee 克隆
git clone https://gitee.com/ssadwlll1988/Flutter.git
cd Flutter
```

#### 步骤 2: 检查 Flutter 环境

```bash
flutter doctor
```

确保看到：
```
[√] Flutter (Channel stable, x.x.x, on Microsoft Windows ...)
[√] Windows Version (...)
[√] Android toolchain - develop for Android devices (可忽略)
[√] Chrome - develop for the web (可忽略)
[√] Visual Studio - develop for Windows apps
[√] Connected device (可忽略)
[√] HTTP Host Availability
```

**注意**：只要 `Visual Studio` 项是绿色的 ✓，其他红色 × 可以忽略。

#### 步骤 3: 获取依赖

```bash
flutter pub get
```

应该看到：
```
Resolving dependencies...
Got dependencies!
```

#### 步骤 4: 运行应用

```bash
flutter run -d windows
```

或者使用简写：
```bash
flutter run
```

首次编译可能需要 2-5 分钟，请耐心等待。

#### 步骤 5: 以管理员身份运行（可选但推荐）

为了能够清理系统目录，建议：

1. 找到生成的 exe 文件：
   ```
   build\windows\x64\runner\Release\c_disk_cleaning_program.exe
   ```

2. 右键点击 → "以管理员身份运行"

或者在调试时：
1. 以管理员身份打开 PowerShell/Command Prompt
2. 再运行 `flutter run -d windows`

---

## 🔧 故障排除

### 问题 1: 编译失败，提示缺少依赖

**症状**：
```
Error: Could not resolve the package 'xxx' in 'package:xxx/xxx.dart'
```

**解决**：
```bash
flutter clean
flutter pub get
flutter run
```

### 问题 2: 运行时崩溃或白屏

**症状**：应用启动后立即关闭或显示白屏

**解决**：
1. 检查是否有未处理的异常：
   ```bash
   flutter run -d windows --verbose
   ```

2. 查看日志文件：
   ```
   flutter_01.log
   ```

3. 尝试重新编译：
   ```bash
   flutter clean
   flutter pub get
   flutter build windows
   flutter run -d windows
   ```

### 问题 3: 无法扫描某些目录

**症状**：扫描结果显示大小为 0 或跳过某些目录

**原因**：权限不足

**解决**：
- 以管理员身份运行应用
- 或者在代码中处理权限异常（已实现）

### 问题 4: 清理后空间没有释放

**症状**：清理完成但磁盘空间未增加

**可能原因**：
1. 文件被其他程序占用
2. 回收站未清空
3. 需要重启资源管理器

**解决**：
- 关闭所有可能占用文件的程序
- 手动清空回收站
- 重启电脑

---

## 📦 打包发布版本

### 生成 Release 版本

```bash
flutter build windows --release
```

生成的文件位于：
```
build\windows\x64\runner\Release\
```

包含的文件：
- `c_disk_cleaning_program.exe` - 主程序
- `data/` - 资源文件
- 各种 `.dll` 文件 - 运行时库

### 创建安装包（可选）

可以使用以下工具创建安装程序：
- [Inno Setup](https://jrsoftware.org/isdl.php)
- [NSIS](https://nsis.sourceforge.io/)
- [Advanced Installer](https://www.advancedinstaller.com/)

---

## ✅ 功能验证清单

在新电脑上运行后，请验证以下功能：

### 基础清理 ✓
- [ ] 点击"开始扫描"能正常扫描
- [ ] 显示 30+ 个清理类别
- [ ] 每个类别显示正确的大小
- [ ] 可以勾选/取消勾选
- [ ] 全选/取消全选功能正常
- [ ] 点击"清理选中项"能正常清理

### 深度扫描 ✓
- [ ] 能扫描日志文件、临时文件等
- [ ] 能发现浏览器缓存
- [ ] 能发现开发工具缓存
- [ ] 扫描进度显示正常

### 大文件管理 ✓
- [ ] 可以设置文件大小阈值
- [ ] 扫描能发现大文件
- [ ] 支持暂停/继续/停止
- [ ] 双击能打开文件所在目录
- [ ] 删除功能正常

### 软件搬家 ✓
- [ ] 能扫描已安装的软件
- [ ] 显示软件信息（名称、大小、路径）
- [ ] 可以选择目标磁盘
- [ ] 搬迁功能正常（需要管理员权限）

### C盘分析 ✓
- [ ] 饼图显示正常
- [ ] 各分类大小计算正确
- [ ] 列表显示详细目录信息

---

## 📞 技术支持

如果遇到问题：

1. 查看日志文件：`flutter_01.log`
2. 运行诊断：`flutter doctor -v`
3. 提交 Issue 到 GitHub
4. 提供以下信息：
   - Flutter 版本：`flutter --version`
   - 错误日志
   - 操作步骤

---

## 🎉 成功标志

如果你能看到以下内容，说明项目可以正常运行：

1. ✅ `flutter doctor` 显示 Visual Studio 为绿色 ✓
2. ✅ `flutter pub get` 成功获取依赖
3. ✅ `flutter run` 成功编译并启动应用
4. ✅ 应用界面正常显示，有 5 个标签页
5. ✅ 基础清理能扫描并显示结果
6. ✅ 点击清理按钮能正常工作

恭喜！你已经成功在另一台电脑上运行了 C盘卫士！🎊
